# Keycloak Configuration
CLIENT_SECRET=*COMPLETAR ACÁ*
CLIENT_ID=aplicacion-demo
REALM=criptografia-y-seguridad-informatica
AUTH_USER=cripto-user
PASSWORD=1234

# URLs
APP_URL="http://localhost:4000"            #URL of the backend application
LOGIN_URL="http://localhost:8080"          #URL of the Identity Provider from the browser (Docker Host)
IDP_URL="http://host.docker.internal:8080" #URL of the Identity Provider from another container

PORT=4000
PKCE=1
