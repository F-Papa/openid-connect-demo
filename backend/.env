# Keycloak Configuration
CLIENT_SECRET=*MODIFICAR ACÁ*
CLIENT_ID=aplicacion-demo
REALM=criptografia-y-seguridad-informatica
AUTH_USER=cripto-user
PASSWORD=1234

# URLs
FRONTEND_URL="http://localhost:3000"       #IP of the frontend application from the browser (Docker Host)
APP_URL="http://localhost:4000"            #IP of the backend application from the browser (Docker Host)
LOGIN_URL="http://localhost:8080"          #IP of the Identity Provider from the browser (Docker Host)
IDP_URL="http://host.docker.internal:8080" #IP of the Identity Provider from the docker network

PORT=4000
PKCE=1
